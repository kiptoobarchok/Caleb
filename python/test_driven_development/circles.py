#!/usr/bin/python3
" simple module to work out area of a circle with different radii "
from math import pi

def area_circle(r):

    area = print(pi * r ** 2)

r = [1, 2.4, "radii", True]

area = (area_circle(r))
print(f"area of circle with radius {r} is {area}")
